| Date | Problem | Topic | Time | Solved unaided? | Repeat | Key insight / why I missed it | Brute Approach | Better Approach | Optimal Approach |
|---|---|---|---|---|---|---|---|---|---|
| 08-18 | Contain Duplicates | Array & Hashing | 16m | No - hint | 0  | You can use set which have a unique property that it contains only unique number it does not contain duplicate | Using nested Loops | Use set and check if it already exist in the set or not | Use set and dump the vector into set and check if the size of set and vector are equal or not |
| 08-18 | Valid Anagram | Array & Hashing | 15m | No - hint | 0 | You can use a fixed array size of 26 because the question specifically says that the string only contains lowercase letter so it will be usefull | Sort them and check if they are equal or not | Make two hash tables and compare them at last if they are equal or not | Take a vector of 26 size filled with zeroes and increment the particular index on the basis of ch from string s and decrement it on the basis of character and at last check if the vector holds value other than 0 if it does then it means the strings are not anagram |
| 08-19 | Two sum | Array & Hashing | 10m | No - hint | 0 | You can use hash map and check if target - the current num is already present in the hash map or not if yes then return the index using the value | Use two nested loops and iterate all over the array to check it there is any element which compromise with the current element | You can sort the array and contains it in a vector of pair with their indexes and if the target is greater than the actual result move the i pointer to right and if it is smaller than the current ans move the j point to the left side | You can use hash map and it contains the actual element as key and the index as their value and find if target - curr is already present in the hash map or not if not then only add it if you found return the indexes. |
| 08-19 | Group Anagrams | Array & Hashing | 22m | No - hint | 0 | You can use hash map to adn store the strings of same type or anagrams as value of particular key | No Brute force | You can sort the string and use it as key in the hash and add the original string as a value in the array of values | You can generate a unique string using the character like if cat is there the unique should be like 101000..... but mind one thing you should generate it with ',' other wise if there are ten same character then it will look like this 101000 rather it should look like this the original value should be 1,0,10,0.. this will help recognize the key easily and less chance of conflict |
| 08-20 | Top k frequent elements |  Array & Hashing | 30m | Min heap - pairs | 0 | You can use a min heap/ Bucket sort is most optimal | You can use vector<pair<int, int>> and sort the vector in descending order then you can find out top k frequent elements | You can use min heap or max heap to replace the vector priority_queue helps us it can automatically keep it in the sort order | Bucket sort is the most optimal way but i have not read it yet i need to read it first then i'll implement it | 
| 08-20 | Product except self | Prefix Sum | 25m | Prefix product and suffix product | 0 |  You can use prefix product array and suffix product array to calcuate the product of the current element | Two nested loops if i == j continue otherwise prod *= nums[j] | we will use this better approach tommorow not now | make two arrays one hold the prefix product and one holds the suffix product the key idea here is to have these product and we can find the current element answer on the basis of prefix and suffix product  There is also an optimal way to do this we will also discover it tommorow |