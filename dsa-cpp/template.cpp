#include <bits/stdc++.h>
using namespace std;

using ll  = long long;
using pii = pair<int,int>;
#define all(x) (x).begin(), (x).end()
#define sz(x)  (int)(x).size()

void solve() {
    // ---- per-testcase logic goes here ----
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);          // ~10x faster I/O — without this you WILL TLE on large inputs
    int t = 1;
    cin >> t;                   // remove if the problem has a single testcase
    while (t--) solve();
    return 0;
}